# This is a Databricks asset bundle definition for reficio.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: reficio
  uuid: 2c8e6853-1d2e-4699-8bc6-afec3796bbcb

artifacts:
  python_artifact:
    type: whl
    path: .
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-8723178682651460.0.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-8723178682651460.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/shaun.ryan@shaunchiburihotmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/workflows/${bundle.name}/${bundle.target}
    permissions:
      - user_name: shaun.ryan@shaunchiburihotmail.onmicrosoft.com
        level: CAN_MANAGE
